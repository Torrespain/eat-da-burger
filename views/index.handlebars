<div class="container">

	<div class="jumbotron">
			<h1> Want a burger?</h1> 
		</div>
	<div class="row" id="imageContainer">
		<img class="pimages" src="https://mir-s3-cdn-cf.behance.net/project_modules/max_1200/cf511510139215.560e0044637c6.jpg" width="250px" height="200px"></a>	
		
	</div>

	<div id="readyHolder" class="row">
		<div class="col-md-6">
			<h3>Ready to eat</h3>
			<div class="row">
				<ul>
					{{#each burgers}}
						{{#unless devoured}}
						<div class="col-md-8">
							<pre>{{this.id}}.{{this.burger_name}}</pre>
						</div>
						<div class="col-md-4">
							<button type="submit" data-id="{{id}}" data-state="0" class="btn btn-primary devourBtn">Devour</button>
						</div>
				    	{{/unless}}
	 				 {{/each}}	
				</ul>
			</div>
			
		</div>
		
		<div id="devouredHolder" class="col-md-6">
			<h3>Devoured so far</h3>
				<div class="row">
					<ul>
						{{#each burgers}}
							{{#if devoured}}
								<div class="col-md-8">
									<pre>{{this.id}}.{{this.burger_name}}</pre>
								</div>
					    	{{/if}}
		 				 {{/each}}	
					</ul>
				</div>
		</div>
	</div>
	<div class="row">
		
			<form class="create-form">
				<div class="form-group">
	    			<label for="burgerInput">Enter the burger you want to add</label>
	    			<textarea class="form-control" id="burgerText" rows="1"></textarea>
	  			</div>
	  			<button type="submit" id="addButton">Add your burger</button>
			</form>	
		
	</div>

</div>